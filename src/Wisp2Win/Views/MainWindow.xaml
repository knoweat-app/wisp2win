<Window x:Class="Wisp2Win.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:Wisp2Win.Models"
        Title="Wisp2Win"
        Width="520"
        Height="560"
        MinWidth="460"
        MinHeight="500"
        Background="{StaticResource SurfaceBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True">
            <StackPanel>
                <TextBlock Text="Wisp2Win"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextBrush}" />
                <TextBlock Text="{Binding Status}"
                           Margin="0,4,0,0"
                           Foreground="{StaticResource MutedTextBrush}" />
            </StackPanel>
            <Button DockPanel.Dock="Right"
                    Width="150"
                    Height="42"
                    Command="{Binding ToggleCommand}"
                    Content="{Binding ToggleText}" />
        </DockPanel>

        <Border Grid.Row="1"
                Margin="0,22,0,0"
                Padding="16"
                CornerRadius="8"
                Background="White"
                BorderBrush="#E2E8F0"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Model" Foreground="{StaticResource MutedTextBrush}" />
                <ComboBox Grid.Row="1"
                          Grid.Column="0"
                          Margin="0,6,12,16"
                          ItemsSource="{Binding Models}"
                          SelectedItem="{Binding SelectedModel}"
                          DisplayMemberPath="DisplayName" />

                <TextBlock Grid.Row="0" Grid.Column="1" Text="Language" Foreground="{StaticResource MutedTextBrush}" />
                <ComboBox Grid.Row="1"
                          Grid.Column="1"
                          Margin="12,6,0,16"
                          SelectedValuePath="Content"
                          SelectedValue="{Binding Language}">
                    <ComboBoxItem Content="ru" />
                    <ComboBoxItem Content="en" />
                    <ComboBoxItem Content="auto" />
                </ComboBox>

                <CheckBox Grid.Row="2"
                          Grid.Column="0"
                          Margin="0,0,12,12"
                          IsChecked="{Binding PasteAfterTranscription}"
                          Content="Paste into active app" />
                <CheckBox Grid.Row="2"
                          Grid.Column="1"
                          Margin="12,0,0,12"
                          IsChecked="{Binding TranslateToEnglish}"
                          Content="Translate to English" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Hotkey" Foreground="{StaticResource MutedTextBrush}" />
                <ComboBox Grid.Row="4"
                          Grid.Column="0"
                          Margin="0,6,12,0"
                          ItemsSource="{Binding Hotkeys}"
                          SelectedItem="{Binding SelectedHotkey}"
                          DisplayMemberPath="DisplayName" />

                <StackPanel Grid.Row="4" Grid.Column="1" Margin="12,0,0,0">
                    <DockPanel>
                        <TextBlock Text="Model download" Foreground="{StaticResource MutedTextBrush}" />
                        <Button DockPanel.Dock="Right"
                                Width="96"
                                Height="28"
                                Command="{Binding DownloadModelCommand}"
                                Content="Download" />
                    </DockPanel>
                    <ProgressBar Height="8"
                                 Margin="0,8,0,0"
                                 Minimum="0"
                                 Maximum="1"
                                 Value="{Binding DownloadProgress}" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Margin="0,16,0,0"
                Padding="12"
                CornerRadius="8"
                Background="#EFF6FF"
                BorderBrush="#BFDBFE"
                BorderThickness="1">
            <TextBlock Text="{Binding HotkeyStatusText}"
                       Foreground="#1D4ED8" />
        </Border>

        <Border Grid.Row="3"
                Margin="0,16,0,0"
                Padding="16"
                CornerRadius="8"
                Background="White"
                BorderBrush="#E2E8F0"
                BorderThickness="1">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Text="Last transcript"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextBrush}" />
                <TextBox Margin="0,10,0,0"
                         Text="{Binding LastTranscript}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         IsReadOnly="True" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
